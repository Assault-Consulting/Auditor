# Frontend
node_modules/
dist/
*.tsbuildinfo

# Tauri
src-tauri/target/
src-tauri/gen/
# Placeholder icons are generated by scripts/gen-placeholder-icons.py in CI
# and locally; they are not source. Remove this line when a real logo lands
# and the icons become committed assets.
src-tauri/icons/

# Python sidecar
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.coverage
coverage.json
htmlcov/
.ruff_cache/

# Artifacts produced by the application while developing — reports,
# evidence bundles and the local witness log must never be committed:
# they carry file paths and chain heads from whatever was being inspected.
/out/
*.pala

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
!.vscode/extensions.json
