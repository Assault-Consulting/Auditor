{
  "components": {
    "schemas": {
      "AdvisoryItemModel": {
        "description": "One thing worth a human's attention that changes no verdict.",
        "properties": {
          "at_seq": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Where, when the item has a location.",
            "title": "At Seq"
          },
          "boot_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Hex boot identifier, when scoped to one.",
            "title": "Boot Id"
          },
          "code": {
            "description": "Stable key, e.g. mono_regression_in_boot.",
            "title": "Code",
            "type": "string"
          },
          "detail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The package's description of this item.",
            "title": "Detail"
          }
        },
        "required": [
          "code",
          "at_seq",
          "boot_id",
          "detail"
        ],
        "title": "AdvisoryItemModel",
        "type": "object"
      },
      "AdvisoryModel": {
        "description": "The advisory channel, carried apart from the verdict on purpose.",
        "properties": {
          "count": {
            "description": "How many items.",
            "title": "Count",
            "type": "integer"
          },
          "items": {
            "description": "The items themselves.",
            "items": {
              "$ref": "#/components/schemas/AdvisoryItemModel"
            },
            "title": "Items",
            "type": "array"
          },
          "note": {
            "default": "advisory items do not affect the verdict",
            "description": "Carried in the payload rather than left to the UI. A consumer that receives advisory items beside chain_ok will eventually treat them as part of it.",
            "title": "Note",
            "type": "string"
          }
        },
        "required": [
          "count",
          "items"
        ],
        "title": "AdvisoryModel",
        "type": "object"
      },
      "ChainResult": {
        "description": "Question one: is what I hold internally consistent?\n\nAlways answerable. It needs no key and no anchor \u2014 only the bytes in\nfront of it \u2014 which is why it is the one question a chain can never\ndecline to answer.",
        "properties": {
          "breaks": {
            "description": "Sequence numbers where prev_hash does not name the predecessor.",
            "items": {
              "type": "integer"
            },
            "title": "Breaks",
            "type": "array"
          },
          "chain_ok": {
            "description": "Whether every record links to its predecessor.",
            "title": "Chain Ok",
            "type": "boolean"
          },
          "count": {
            "description": "Records the verifier walked.",
            "title": "Count",
            "type": "integer"
          },
          "gaps": {
            "description": "Skipped sequence numbers. A gap is a break whether or not the hashes on either side happen to link.",
            "items": {
              "type": "integer"
            },
            "title": "Gaps",
            "type": "array"
          },
          "head": {
            "description": "Hex digest of the last record in the chain.",
            "title": "Head",
            "type": "string"
          },
          "uninterpretable": {
            "description": "Records with an unknown format version or record type. Chain-checked and reported, never rejected \u2014 the verifier not understanding a record is not the same as the record being wrong.",
            "items": {
              "type": "integer"
            },
            "title": "Uninterpretable",
            "type": "array"
          },
          "violations": {
            "description": "(seq, reason) pairs for normative MUSTs the record broke. Kept as pairs: a seq without its reason is a number nobody can act on.",
            "items": {
              "items": {},
              "type": "array"
            },
            "title": "Violations",
            "type": "array"
          }
        },
        "required": [
          "chain_ok",
          "count",
          "head",
          "breaks",
          "gaps",
          "violations",
          "uninterpretable"
        ],
        "title": "ChainResult",
        "type": "object"
      },
      "ChainSubject": {
        "description": "What the container *is*, stated before any verdict about it.\n\nSeparate from the verification result on purpose: a reader of a report\nmust be able to confirm they are holding the same artifact the check ran\nagainst, independently of whether that check passed.",
        "properties": {
          "boots": {
            "description": "Distinct boot identifiers present.",
            "title": "Boots",
            "type": "integer"
          },
          "bytes": {
            "description": "File size on disk, in bytes.",
            "title": "Bytes",
            "type": "integer"
          },
          "filename": {
            "description": "The file's name, for display only.",
            "title": "Filename",
            "type": "string"
          },
          "first_seq": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Lowest sequence number present.",
            "title": "First Seq"
          },
          "last_seq": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Highest sequence number present.",
            "title": "Last Seq"
          },
          "path": {
            "description": "Absolute path as opened.",
            "title": "Path",
            "type": "string"
          },
          "records": {
            "description": "Number of records the reader found.",
            "title": "Records",
            "type": "integer"
          },
          "sha256": {
            "description": "SHA-256 of the file as opened. Identifies the artifact; a report naming only a filename identifies nothing.",
            "title": "Sha256",
            "type": "string"
          },
          "spans": {
            "description": "Distinct spans present.",
            "title": "Spans",
            "type": "integer"
          }
        },
        "required": [
          "filename",
          "path",
          "bytes",
          "sha256",
          "records",
          "first_seq",
          "last_seq",
          "boots",
          "spans"
        ],
        "title": "ChainSubject",
        "type": "object"
      },
      "Completeness": {
        "description": "Question two: is what I hold all of it?\n\nThe only question that can go unasked, and the answer must say so.",
        "properties": {
          "anchor_lag": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Records present beyond the anchored head, when there are any.",
            "title": "Anchor Lag"
          },
          "anchor_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The verifier's sentence explaining an incomplete answer.",
            "title": "Anchor Reason"
          },
          "complete_to_anchor": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "description": "True, False, or null. Null means NO ANCHOR WAS SUPPLIED and the question was never asked \u2014 it is not a pass and must never be rendered as one.",
            "title": "Complete To Anchor"
          }
        },
        "required": [
          "complete_to_anchor",
          "anchor_lag",
          "anchor_reason"
        ],
        "title": "Completeness",
        "type": "object"
      },
      "DiagnosisModel": {
        "description": "What the failure looks like, rather than that there was one.\n\nThe pattern is the machine-readable part and a consumer may key visuals\noff it. The narrative is the package's own sentence, carried verbatim:\na shell may show a localised sentence beside it, never instead of it, or\nthe report stops saying what the verifier said.",
        "properties": {
          "at_seq": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Where, when the pattern has a location.",
            "title": "At Seq"
          },
          "expected": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "What the verifier expected to find.",
            "title": "Expected"
          },
          "narrative": {
            "description": "The verifier's own description, verbatim.",
            "title": "Narrative",
            "type": "string"
          },
          "pattern": {
            "description": "One of: truncated_tail, prefix_absent, seq_gap, chain_break, record_violation, unanchored_tail, replaced_or_rolled_back.",
            "title": "Pattern",
            "type": "string"
          }
        },
        "required": [
          "pattern",
          "at_seq",
          "expected",
          "narrative"
        ],
        "title": "DiagnosisModel",
        "type": "object"
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "title": "Detail",
            "type": "array"
          }
        },
        "title": "HTTPValidationError",
        "type": "object"
      },
      "HealthResponse": {
        "description": "Liveness, and the identity of the verifier behind this service.\n\nThe verifier identity is part of the liveness answer rather than a\nseparate endpoint because a verification result is only meaningful\nalongside the verifier that produced it. A shell that knows the service is\nup but not what it is linked against knows less than it needs.",
        "properties": {
          "authenticated": {
            "description": "Whether the session token gate is enforced. False means the sidecar was started without a token and any local process can reach it \u2014 a development affordance, never a supported configuration.",
            "title": "Authenticated",
            "type": "boolean"
          },
          "package": {
            "description": "The verifier package and version, e.g. 'palimpsests 0.8.0'.",
            "title": "Package",
            "type": "string"
          },
          "spec": {
            "description": "The wire format the linked verifier implements, e.g. 'PALA-1 format_version 1'.",
            "title": "Spec",
            "type": "string"
          },
          "status": {
            "description": "'ok' when the service is serving.",
            "title": "Status",
            "type": "string"
          },
          "version": {
            "description": "Version of this sidecar.",
            "title": "Version",
            "type": "string"
          }
        },
        "required": [
          "status",
          "version",
          "package",
          "spec",
          "authenticated"
        ],
        "title": "HealthResponse",
        "type": "object"
      },
      "SessionRequest": {
        "description": "Open a container.",
        "properties": {
          "path": {
            "description": "Absolute path to a .pala container.",
            "title": "Path",
            "type": "string"
          }
        },
        "required": [
          "path"
        ],
        "title": "SessionRequest",
        "type": "object"
      },
      "SessionResponse": {
        "description": "A session over one open container.",
        "properties": {
          "session_id": {
            "description": "Opaque handle for subsequent calls.",
            "title": "Session Id",
            "type": "string"
          },
          "subject": {
            "$ref": "#/components/schemas/ChainSubject",
            "description": "What this session is about."
          },
          "verifier": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "The verifier package and wire format behind this session. Carried on the session rather than fetched separately, because a result is only meaningful alongside the verifier that produced it.",
            "title": "Verifier",
            "type": "object"
          }
        },
        "required": [
          "session_id",
          "subject",
          "verifier"
        ],
        "title": "SessionResponse",
        "type": "object"
      },
      "ValidationError": {
        "properties": {
          "ctx": {
            "title": "Context",
            "type": "object"
          },
          "input": {
            "title": "Input"
          },
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "title": "Location",
            "type": "array"
          },
          "msg": {
            "title": "Message",
            "type": "string"
          },
          "type": {
            "title": "Error Type",
            "type": "string"
          }
        },
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError",
        "type": "object"
      },
      "VerificationResponse": {
        "description": "The verifier's answer, passed through rather than summarised.\n\nThere is deliberately no single \"valid\" field. The three questions have\nthree separate answers, one of which can be \"not asked\", and any field\nthat collapsed them would be the shell deciding what a verdict means.",
        "properties": {
          "advisory": {
            "$ref": "#/components/schemas/AdvisoryModel"
          },
          "chain": {
            "$ref": "#/components/schemas/ChainResult"
          },
          "completeness": {
            "$ref": "#/components/schemas/Completeness"
          },
          "diagnosis": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DiagnosisModel"
              },
              {
                "type": "null"
              }
            ],
            "description": "Present only when something failed."
          },
          "session_id": {
            "description": "The session this answer is about.",
            "title": "Session Id",
            "type": "string"
          },
          "subject_sha256": {
            "description": "Digest of the file this answer describes. Repeated here so a verification result cannot be separated from its subject.",
            "title": "Subject Sha256",
            "type": "string"
          },
          "verifier": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Package and wire format behind it.",
            "title": "Verifier",
            "type": "object"
          }
        },
        "required": [
          "session_id",
          "subject_sha256",
          "verifier",
          "chain",
          "completeness",
          "diagnosis",
          "advisory"
        ],
        "title": "VerificationResponse",
        "type": "object"
      }
    }
  },
  "info": {
    "title": "palimpsests-auditor-sidecar",
    "version": "0.0.1"
  },
  "openapi": "3.1.0",
  "paths": {
    "/health": {
      "get": {
        "description": "Liveness probe, and the identity triple the UI displays.\n\nThe verifier version is reported because a verification result is only\nmeaningful alongside the verifier that produced it.",
        "operationId": "health_health_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Health"
      }
    },
    "/session": {
      "post": {
        "description": "Open a container and return what it is.\n\nDeliberately says nothing about whether the chain verifies. Identity\nand structure are established first, and separately: a reader of a\nreport must be able to confirm they hold the same artifact the check\nran against, whether or not that check passed.",
        "operationId": "open_session_session_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SessionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Open Session"
      }
    },
    "/session/{session_id}": {
      "delete": {
        "operationId": "close_session_session__session_id__delete",
        "parameters": [
          {
            "in": "path",
            "name": "session_id",
            "required": true,
            "schema": {
              "title": "Session Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Close Session"
      },
      "get": {
        "operationId": "get_session_session__session_id__get",
        "parameters": [
          {
            "in": "path",
            "name": "session_id",
            "required": true,
            "schema": {
              "title": "Session Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Session"
      }
    },
    "/session/{session_id}/verify": {
      "get": {
        "description": "The verifier's answer about this session's container.\n\nRe-checks the file digest first. A verdict about bytes that have since\nchanged is worse than no verdict, because it looks like one.\n\nNote what this endpoint does NOT return: a single \"valid\" field. The\nthree questions have three separate answers, one of which can be \"not\nasked\", and collapsing them here would be the shell deciding what a\nverdict means \u2014 which is the one thing ADR-0001 exists to prevent.",
        "operationId": "verify_session_session__session_id__verify_get",
        "parameters": [
          {
            "in": "path",
            "name": "session_id",
            "required": true,
            "schema": {
              "title": "Session Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VerificationResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Verify Session"
      }
    }
  }
}
